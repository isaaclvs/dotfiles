# 🎵 Media Controls Keybindings - Audio, Video & Screenshots
# Multimedia keys, volume, brightness, and screenshot controls
# Hardware function keys and media controls

######################
### MEDIA CONTROLS ###
######################

# ===== SCREENSHOTS =====
# Print Screen → Screenshot do monitor em foco (INTELIGENTE!)
bind = , Print, exec, ~/.config/hypr/scripts/screenshot.sh focused

# Shift + Print Screen → Selecionar área (qualquer monitor)
bind = SHIFT, Print, exec, ~/.config/hypr/scripts/screenshot.sh area

# Ctrl + Print Screen → Área para clipboard
bind = CTRL, Print, exec, ~/.config/hypr/scripts/screenshot.sh clipboard

# Alt + Print Screen → Screenshot da janela ativa
bind = ALT, Print, exec, ~/.config/hypr/scripts/screenshot.sh window

# Super + Print Screen → Monitor em foco para clipboard
bind = $mainMod, Print, exec, ~/.config/hypr/scripts/screenshot.sh clipboard-focused

# Super + Shift + Print → Todos os monitores (raramente usado)
bind = $mainMod SHIFT, Print, exec, ~/.config/hypr/scripts/screenshot.sh all

# ===== VOLUME CONTROLS =====
# Controles de volume (teclas F-keys + multimedia)
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# ===== BRIGHTNESS CONTROLS =====
# Controles de brilho (F1/F2 no seu notebook)
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# ===== LAPTOP LID CONTROL =====
# Controle inteligente da tampa do notebook
bindl = ,switch:on:Lid Switch, exec, ~/.config/hypr/scripts/handle_lid_switch.sh
bindl = ,switch:off:Lid Switch, exec, hyprctl keyword monitor "eDP-1, enable"

# ===== MEDIA PLAYER CONTROLS =====
# Controles de música (requer playerctl)
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# ========== OBSERVAÇÕES ==========
# Screenshots inteligentes:
# - Print: Detecta automaticamente o monitor em foco
# - Shift+Print: Seleção de área com mouse
# - Ctrl+Print: Área direto para clipboard
# - Alt+Print: Janela ativa apenas
#
# Volume e brilho:
# - Usa wpctl (PipeWire) para audio
# - Usa brightnessctl para brilho
# - Funciona com teclas F do notebook
#
# Laptop lid control:
# - switch:on = tampa fechada → executa script handle_lid_switch.sh
# - switch:off = tampa aberta → reativa monitor eDP-1
# - Script handle_lid_switch.sh gerencia suspend/lock
#
# Media controls:
# - Funciona com YouTube Music, Spotify, etc.
# - Requer playerctl instalado
# - Teclas dedicadas de mídia do teclado
#
# Para debug:
# - Screenshots: ~/.config/hypr/scripts/screenshot.sh
# - Volume: wpctl status
# - Brilho: brightnessctl get
# - Lid switch: ~/.config/hypr/scripts/handle_lid_switch.sh
# - Media: playerctl status
