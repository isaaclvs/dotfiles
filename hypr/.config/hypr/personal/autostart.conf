# 🚀 Autostart Configuration - Boot Programs
# Programs that start automatically with Hyprland
# Order matters for some applications

#################
### AUTOSTART ###
#################
# exec-once = executa apenas uma vez por sessão
# exec = executa toda vez que o config é recarregado

# ========== CORE SYSTEM ==========
# Notificações (sempre primeiro)
exec-once = dunst &

# Sistema de gerenciamento de energia
exec-once = hypridle

# ========== INTERFACE ==========
# Barra de status
exec-once = waybar &

# Wallpaper daemon
exec-once = swww-daemon &

# Luz noturna automática
exec-once = wlsunset -l -5.0892 -L -42.8019

# ========== APLICACOES PRINCIPAIS ==========
# Browser principal (sempre disponível)
exec-once = brave &

# ========== WALLPAPER ==========
# Aguarda 3s para garantir que swww-daemon iniciou
exec-once = swww-daemon && sleep 3 && swww img ~/Pictures/Wallpapers/forest.png --transition-type fade --transition-duration 1

# ========== CLIPBOARD ==========
# Sistema de histórico do clipboard
# Aguarda 2s para garantir que Wayland está pronto
exec-once = sleep 2 && wl-paste --watch cliphist store &

# ========== NETWORK & CONNECTIVITY ==========
# NetworkManager system tray (WiFi, VPN, etc.)
exec-once = nm-applet --indicator &

# ========== OBSERVAÇÕES ==========
# - dunst: Sistema de notificações deve vir primeiro
# - waybar: Barra de status com todos os módulos
# - swww: Wallpaper dinâmico para Wayland
# - cliphist: Histórico do clipboard persistente
# - brave: Browser sempre disponível no boot
# - nm-applet: Ícone de rede na system tray
#
# Para debug de autostart:
# journalctl --user -b | grep hyprland
#
# Para verificar processos:
# ps aux | grep -E "waybar|dunst|swww|cliphist"
